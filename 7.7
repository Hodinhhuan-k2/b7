print("Hồ Đình Huân")
print("MSSV:235752021610049")
def count_lines_in_file(fname):
    try:
        with open(fname, 'r') as file:
            lines = file.readlines()
            print(f"Số dòng trong tệp: {len(lines)}")
    except FileNotFoundError:
        print(f"File '{fname}' không tồn tại.")

# Gọi hàm và nhập tên tệp cần đếm dòng
file_path = input("Nhập tên tệp: ")
count_lines_in_file(file_path)
